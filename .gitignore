# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri updater signing keys — NEVER commit these
# (the public key lives in src-tauri/tauri.conf.json; no key files belong in git)
*.key
*.pub
.tauri-keys/

# Internal maintainer release runbook (kept local, not published)
docs/RELEASING.md
